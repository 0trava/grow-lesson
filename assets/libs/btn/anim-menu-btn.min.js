!function () { 
    var t = document.getElementsByClassName("anim-menu-btn"); if (0 < t.length) { for (var a = 0; a < t.length; a++)!function (n) { var e; (e = t[a]).addEventListener("click", function (n) { n.preventDefault(); var t = !e.classList.contains("anim-menu-btn--state-b"); e.classList.toggle("anim-menu-btn--state-b", t); var n = new CustomEvent("anim-menu-btn-clicked", { detail: t }); e.dispatchEvent(n) }) }() } }();


    document.addEventListener("DOMContentLoaded", function () {
        var menuButtonOpen = document.querySelector(".anim-menu-btn");
        var menuButtonClose = document.querySelector(".anim-menu-btn-close");
        var modalMenu = document.querySelector(".modal-menu");
    
        if (menuButtonOpen && modalMenu) {
            menuButtonOpen.addEventListener("click", function (event) {
                event.preventDefault();
                modalMenu.classList.toggle("modal-menu--open");
                menuButtonOpen.classList.toggle("anim-menu-hide");
                menuButtonClose.classList.toggle("anim-menu-btn--state-b");
            });

        }

        if (menuButtonClose && modalMenu) {
                menuButtonClose.addEventListener("click", function (event) {
                    event.preventDefault();
                    modalMenu.classList.remove("modal-menu--open");
                    menuButtonOpen.classList.remove("anim-menu-hide");
                    menuButtonClose.classList.remove("anim-menu-btn--state-b");
                    menuButtonOpen.classList.remove("anim-menu-btn--state-b");
            });
        }

    });